#pragma once

template<int size, const char (&arr)[size]>
struct solution {
    constexpr static int answer = 0;
};

template<int size, const char (&arr)[size]>
struct solution_pt2 {
    constexpr static int answer = 0;
};
